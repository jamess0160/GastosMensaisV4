/** Só DECLARA os eventos da tela — o corpo de cada um vive em
 *  ./sections, um arquivo por evento.
 *
 *  Sem eventos, como o Relatório: o extrato é leitura pura. Ele não
 *  quita, não concilia e não marca nada — a única coisa que se faz aqui
 *  além de ler é NAVEGAR até o lançamento, e navegar é do roteador, não
 *  um evento de negócio. */
class Controller {}

export const StatementController = new Controller();
